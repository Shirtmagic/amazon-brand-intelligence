import agents from '@/data/agents.json';
import kanban from '@/data/kanban.json';
import apiUsage from '@/data/api-usage.json';
import brands from '@/data/brands.json';
import palettes from '@/data/palettes.json';
import dependencies from '@/data/dependencies.json';
import activity from '@/data/activity.json';
import models from '@/data/models.json';

export const mockAgents = agents;
export const mockKanban = kanban;
export const mockApiUsage = apiUsage.providers;
export const mockBrands = brands;
export const mockPalettes = palettes;
export const mockDependencies = dependencies;
export const mockActivity = activity;
export const modelOptions = models;
